name: Ruff
on:
  pull_request:
  push:
    branches: [main]
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/ruff-action@v4.1.0
        with:
          version: "0.15.20"
          args: "--version"
      - run: ruff check
      - run: ruff format --check
